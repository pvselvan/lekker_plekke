<h1>New Plekke!</h1>

<%= form_for @place do |f| %>
	<p>
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.label :address %><br>
		<%= f.text_field :address %>
	</p>

	<p>
		<%= f.submit %>
	</p>

<% end %>

<%= link_to 'Back', places_path %>